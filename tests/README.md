# Tests

Run all tests:

```bash
python -m unittest discover -s tests -v
```

Current coverage focus:
- RSS parser date/id/html extraction
- Detail parser title/organism/buyer extraction
- Normalization date conversion
- Scoring include/exclude behavior
